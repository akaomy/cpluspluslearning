#include <iostream>
using namespace std;

int main () {
    int one = 1;
    int another = 2;
    string myCondition;
    string correct = "that's right";
    string wrong = "wrong one";

    // myCondition = (one == another) ? correct : wrong;  
    myCondition = (one == another) ? "that's right" : "wrong one"; 
    // cout << correct << endl;
    // cout << wrong << endl;
}