#include <iostream>
using namespace std;

int main () {
    // to delete or inser an element in 
    // the middle of linkedlist:
    // we're just relinking pointers 
    // between needed nodes
    // Random Access:           O(n)
    // Insertion at end:        O(1)
    // Insertion in the middle: O(1)
    // Cache locally:           no

    return 0;
}